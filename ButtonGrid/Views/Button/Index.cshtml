<!--
    /*
    * Obadiah Fusco
    * Activety 4
    * CST-350
    * 10/26/25
    */
-->
@model IEnumerable<ButtonGrid.Models.ButtonModel>

@{
    ViewData["Title"] = "Index";
}


<form>
    <div class="button-zone">
        @* iterate over buttons to display them*@
        @for (int i = 0; i < Model.Count(); i++)
        {
            if (@i % 5 == 0)
            {
                <div class="line-break"></div>
            }
            <partial name="_OneButton" model="Model.ElementAt(i)" />
        }
    </div>
</form>

